#include <pthread.h>
#include <stdio.h>
#include <stdlib.h>
#include <fstream>
using namespace std;

int main(int argc, char *argv[])
{
    pthread_t thread1, thread2;
    int iret1, iret2;

    
    exit(0);
}